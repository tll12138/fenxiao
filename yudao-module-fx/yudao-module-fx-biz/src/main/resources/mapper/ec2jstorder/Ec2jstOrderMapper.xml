<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.fx.dal.mysql.ec2jstorder.Ec2jstOrderMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <update id="updateBatchById">
        <foreach collection="list" item="item" index="index" separator=";">
            UPDATE fx_ec2jst_order
            <set>
                <if test="item.toErpTime != null">
                    to_erp_time = #{item.toErpTime},
                </if>
                <if test="item.erpStatus != null">
                    erp_status = #{item.erpStatus},
                </if>
                <if test="item.oId != null">
                    o_id = #{item.oId}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
</mapper>