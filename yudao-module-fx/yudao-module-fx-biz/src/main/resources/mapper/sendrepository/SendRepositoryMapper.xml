<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.fx.dal.mysql.sendrepository.SendRepositoryMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <update id="updateOtherInfo">
        UPDATE fx_inventory_data t
            LEFT JOIN fx_send_repository u1 ON u1.code = t.warehouse_code
            LEFT JOIN fx_goods_archives u2 ON u2.sku_id = t.sku_id
        SET t.type    = u1.type,
            t.good_id = u2.id,
            t.value   = u2.properties_value
        WHERE t.warehouse_code IS NOT NULL
          AND t.sku_id IS NOT NULL
    </update>
</mapper>